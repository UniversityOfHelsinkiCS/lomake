//import { testFacultyCode, defaultYears } from '../../config/common'

//describe('Overview page tests', () => {
  //const cypressSuperAdmin = 'cypressSuperAdminUser'

  //beforeEach(() => {
    //cy.login(cypressSuperAdmin)
    //cy.visit(`/admin`)
    //cy.contains('Deadline settings').click()
    //cy.createDeadline(defaultYears[0], 'Toimenpiteiden toteutus ja seuranta tiedekunnissa')
    //cy.get('[data-cy=form-8-deadline]').contains('14.')
    //cy.visit(`/faculty-monitoring`)
  //})

  //it('Overview page should not be empty', () => {
    //cy.request('/api/cypress/createFacultyAnswers/8')
    //cy.reload()

    //cy.contains('Development Area 1:').click()
    //cy.get(`[data-cy=square-${testFacultyCode}-1]`).should('have.css', 'background-color', 'rgb(157, 254, 156)')

    //cy.contains('Development Area 7:').click()
    //cy.get(`[data-cy=square-${testFacultyCode}-29]`).should('have.css', 'background-color', 'rgb(157, 254, 156)')
  //})
//})
